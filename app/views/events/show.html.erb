<p id="notice"><%= notice %></p>

<div class="wrap">
<h1>確認画面</h1>

  <div class="cal_entry">
    <p class="cal_entry--date">
      <strong>日付:</strong>
      <%= @event.start_date.strftime('%Y/%m/%d') %>
    </p>

    <p class="cal_entry--title">
      <strong>内容:</strong>
      <%= @event.title %>
    </p>
  </div>
</div>

<div class="nav_three">
  <i class="fas fa-caret-right"></i>
  <%= link_to '編集画面へ戻る', edit_event_path(@event) %> |
  <%= link_to '編集リストへ戻る', events_path %> |
  <%= link_to 'トップ画面へ', root_path %>
</div>

